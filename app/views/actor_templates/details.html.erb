<h1>
  Actor <%= @the_actor.id %> details
</h1>

<a href="/actors" class="btn btn-block btn-outline-secondary">
  Go back
</a>

&nbsp;|&nbsp;

<a href="/delete_actor/<%= @the_actor.id %>" class="btn btn-block btn-outline-secondary">
  Delete actor
  <small>(Building this link is not required. It's an optional stretch goal; but, you have the tools if you want to give it a try.)</small>
</a>

<hr>

<dl>
  <dt>
    Name
  </dt>
  <dd>
    <%= @the_actor.name %>
  </dd>

  <dt>
    Dob
  </dt>
  <dd>
    <%= @the_actor.dob %>
  </dd>

  <dt>
    Bio
  </dt>
  <dd>
    <%= @the_actor.bio %>
  </dd>

  <dt>
    Image
  </dt>
  <dd>
    <img src="<%= @the_actor.image %>" style="width:250px;">
  </dd>

  <dt>
    Created at
  </dt>
  <dd>
    <%= time_ago_in_words(@the_actor.created_at) %> ago
  </dd>

  <dt>
    Updated at
  </dt>
  <dd>
    <%= time_ago_in_words(@the_actor.updated_at)%> ago
  </dd>
</dl>

<h1>  Filmography  </h1>  

<table border="1">
    <tr>
      <th>  Title    </th>
      <th>  Year    </th>
      <th>  Director    </th>
      <th>  Character Name    </th>
      <th>      </th>
    </tr>

    <%= @characters.each do |a_character|%>
      <% movie_id = a_character.movie_id %> 

        <tr>
          <td> 
            <%= Movie.all.where({:id => movie_id}).at(0).title%>  
          </td>

          <td> 
            <%= Movie.all.where({:id => movie_id}).at(0).year%>  
          </td>

          <td> 
            <% d = Movie.all.where({:id => movie_id}).at(0).director_id %>  
            <%= Director.all.where({:id => d}).at(0).name %>
          </td>

          <td> 
            <%= a_character.name %>  
          </td>

          <td> 
            <a href="/movies/<%= movie_id %>" > Show details </a>
          </td>


        </tr>

    <% end %>



</table>
